import Calendar from "../components/calendar/Calendar";

const Schedule = () => {

  return (
    <div className="w-[90%] max-w-7xl mx-auto h-screen overflow-auto scrollbar-hide">
      <Calendar />
    </div>
  );
};

export default Schedule;
